/*
 * native-ble.js — Stub para la versión WEB.
 *
 * En la web no hay Bluetooth nativo, así que aquí no se define window.ScaleBLE.
 * En la build de la APK, este archivo se SUSTITUYE por el módulo real
 * (empaquetado desde src/native-ble.mjs con esbuild). Ver BUILD_APK.md.
 */
/* Sin Bluetooth nativo en el navegador: window.ScaleBLE queda indefinido. */
